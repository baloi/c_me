#define CATCH_CONFIG_MAIN  // tells catch to provide main (only in one file)

#include "catch.hpp"
#include "adb.cpp"

TEST_CASE("Database class creation", "[factorial]") {
    //REQUIRE( Factorial(1) == 1);
}
